version: '3'

services:

  capricorn_db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: example

  capricorn_web:
    build: .
    entrypoint: /code/entrypoint.sh
    volumes:
      - .:/code
    ports:
      - "8005:8005"
    depends_on:
      - capricorn_db
