version: '3'

services:

  ursa_major_db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: example

  ursa_major_web:
    build: .
    entrypoint: /code/entrypoint.sh
    volumes:
      - .:/code
    ports:
      - "8005:8005"
    depends_on:
      - ursa_major_db
